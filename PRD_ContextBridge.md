# ContextBridge POC PRD

> 版本：v0.1（本地演示版）  
> 状态：可用于前端实现与后续技术方案设计  
> 产品名：ContextBridge — 跨角色上下文转换台

## 1. 文档目的

ContextBridge 是一个面向企业团队的跨角色上下文转换产品。它把任意源角色在工作中积累的 Agent 记忆、知识、离线资料、线上资料和待办，**有边界地转换**为任意目标角色能够理解、检查和继续执行的工作记忆。

本次 POC 使用“售前人员 → FDE（客户交付工程师）”作为预置演示案例：这两个角色的语言与责任边界反差足够大，最适合在短视频中展示转换价值。但产品模型不绑定该角色组合；角色差异由源角色与目标角色的 Role Profile 决定。

本 PRD 定义一个可在本地运行、可在约 3 分钟内完整演示的 POC。它优先追求：

1. 观看者能快速感受到任意源角色与目标角色之间的“上下文鸿沟”可被填平；
2. 页面效果足够像一个完整的企业产品，而不是聊天框或文档摘要；
3. 所有业务数据均内置为 Mock 数据，不依赖真实 CRM、项目管理、SSO 或文档系统；
4. 前端与本地服务可低复杂度部署和复现；
5. Agent 的判断、边界、人工确认和验证过程清晰可见。

本 POC 不试图替代 CRM、项目管理或实施交付系统，也不承诺真实生产集成。

---

## 2. 背景、问题与产品定位

### 2.1 业务问题

售前角色与 FDE 的工作语言不同：

| 售前的工作语言 | FDE 的工作语言 |
|---|---|
| 客户痛点、业务价值、预算、关键人、成交节奏 | 范围、架构、依赖、验收标准、风险、实施计划 |
| “客户希望 3 周上线” | “上线范围、数据、权限、环境是否已满足？” |
| “支持 SSO、接 CRM” | “使用哪一种 IdP？字段、权限和接口是否已确认？” |
| “客户高层很关注效果” | “谁是业务 Owner？怎样定义可验收的成功指标？” |

现实中跨角色交接往往依赖手写交接文档、口头同步或直接转发一批原始资料。结果是：

- 关键承诺、客户偏好和风险会遗漏；
- 尚未验证的销售判断会被 FDE 当成事实；
- 报价、竞争情报、凭据等不应传递的信息被带入交付；
- 接手角色需要从头再问一遍，工作启动慢且容易发生误解。

### 2.2 产品定位

> **ContextBridge 将源角色的碎片化工作上下文，转换为目标角色可检查、可修改、可执行的工作记忆。**

预置案例中的具体表达为：将售前的“商机与客户需求记忆”，转换为 FDE 可执行的“交付上下文、风险与启动计划”。

它的输出不是一篇摘要，而是一份有来源、状态和边界的 `FDE Deployment Brief`：

- 已确认的客户目标、范围与关键人；
- 被重新表述为技术/交付语言的约束和需求；
- 需要验证的假设与缺失项；
- 未履行的承诺与实施风险；
- FDE 第一个工作周可执行的行动计划；
- 不随交接流转的敏感内容及其原因。

### 2.3 核心价值主张

| 对象 | 价值 |
|---|---|
| 源角色 | 减少手工整理交接材料；清楚哪些信息不能被直接传递。 |
| 目标角色 | 不用阅读全部原始资料；获得可检查、可修订的行动上下文。 |
| 团队负责人 | 更早识别上下文缺口、未经验证的承诺和责任不清的问题。 |
| 企业 | 减少跨团队上下文断层，缩短工作接手时间，降低协作失真风险。 |

---

## 3. POC 范围

### 3.1 P0：本次必须实现

1. 预加载一个完整、可信的客户交接场景；
2. 展示源角色“售前人员”和目标角色“FDE”的职责差异，并以通用 Role Profile 形式呈现；
3. 提供“上下文选择器”，让用户可从 Agent 记忆、知识、离线文件、线上文件快照中勾选本次转换输入；
4. 展示原始资料、抽取后的记忆条目、转换规则、目标记忆之间的可追溯映射；
5. 将每条信息分类为：迁移、重构、待人工确认、拦截、忽略；
6. 让用户处理至少两条需要人工确认的信息；
7. 让目标角色 FDE 对转换结果进行检查、修改和确认；
8. 生成并展示 FDE Deployment Brief；
9. 使用“接收方验证”回答四个预置问题，显示接手就绪度；
10. 展示本次交接的运行轨迹和审计摘要；
11. 能在本地一键启动、重置和重复演示。

### 3.2 P1：如时间允许再实现

- 导出 Markdown / JSON 交接包；
- 读取用户选择的真实 `.md`、`.txt`、`.json` 文件或真实线上连接器；
- 展示实际模型调用的耗时、Token 与成本估算；
- 展示交接包版本历史。

### 3.3 明确不做

- 登录、组织/租户、多用户协作；
- 真实 CRM、邮件、项目管理、身份系统或生产环境的读写；本期线上资料仅为本地保存的连接器快照；
- 任意 PDF/OCR 解析、向量数据库、复杂权限系统；
- 自动向客户承诺日期、报价或自动启动部署；
- 通用聊天机器人页面。

---

## 4. 用户、角色与使用情境

### 4.1 演示操作用户

**交接发起人（Demo Operator）**：选择本次要使用的上下文，审阅 Agent 的转换结果，解决有争议的项目事实，并发起交接。

### 4.2 源角色：售前人员（Sales / Solutions Consultant）

售前拥有客户业务目标、决策链、采购节奏、会议原话、方案表述和口头承诺等上下文。他的资料可信度不一，且会混有个人判断、竞争信息和商业敏感信息。

### 4.3 目标角色：FDE

FDE 接手后需要安排技术发现会、确认实施范围、评估环境与依赖、建立验收标准、发现交付风险并启动试点。他不需要完整的商机叙事，而需要经过验证的、可行动的交付上下文。

### 4.4 通用角色模型

ContextBridge 不把“售前”和“FDE”写死在产品逻辑里。每个角色由一张可配置的 `Role Profile` 描述：

- 源角色：我拥有哪些上下文、哪些内容可能敏感；
- 目标角色：我需要回答哪些问题、可接收哪些信息、首要行动是什么；
- 迁移策略：哪些内容直接迁移、重构、拦截或必须人工确认。

因此，同一个产品模型可支持“客户支持 → 客户成功”“产品经理 → 研发负责人”“一线值班 → 下一班值班”等组合。本期界面会保留源/目标角色选择和角色卡，但只为“售前 → FDE”内置完整的可运行 Mock 数据，以控制实现复杂度。

### 4.5 Mock 场景

**客户：澄澈零售集团**  
**产品：AtlasFlow 企业知识与流程智能助手**  
**项目：面向客服与门店运营团队的 300 人试点**

零售场景的业务表达更直观、Mock 数据也更容易理解：客服和门店运营人员需要在运营手册、售后工单中快速找到处理方法。客户要求接入内部运营手册、Salesforce 服务工单，并支持 Azure AD SSO；同时偏好在内网环境试点。售前在沟通中提出“约三周内可以上线”。FDE 在项目正式启动前需要判断什么已确认、什么只是需求或承诺、什么必须补齐。

---

## 5. 产品原则与信息处理规则

### 5.1 不是摘要，而是角色化转换

每一条源信息必须具备来源、类型、可信度、敏感等级和迁移动作；所有落入目标记忆的内容必须能回溯到原始材料。

### 5.2 记忆类型

| 类型 | 定义 | 示例 |
|---|---|---|
| 已确认事实 | 有正式资料或明确会议结论支持 | 首批试点 300 人，覆盖两个区域团队。 |
| 客户目标 | 客户希望实现的业务结果 | 将门店/售后问题处理时的信息检索由约 4 小时降到 1 小时。 |
| 需求/约束 | 期望、范围或部署限制 | 要求 Azure AD SSO；偏好内网部署。 |
| 承诺 | 已对客户表达的行动或日期 | 售前已称可在 3 周内完成试点上线。 |
| 假设/主观判断 | 未验证的推测或个人印象 | Sponsor 似乎对项目缺乏信心。 |
| 风险/依赖 | 会影响交付的已知条件 | CRM 字段和权限模型尚未确认。 |
| 敏感信息 | 不应直接迁移给 FDE 的商业或安全信息 | 折扣底线、竞争对手策略、测试凭据。 |

### 5.3 迁移动作

| 动作 | 适用条件 | FDE 可见结果 |
|---|---|---|
| `transfer` 直接迁移 | 已确认且与交付相关 | 作为正式上下文写入目标记忆。 |
| `reframe` 重构迁移 | 内容真实但源语言不适用于 FDE | 转成范围、风险、验收或行动项。 |
| `review` 待人工确认 | 冲突、对外承诺、主观判断、高影响不确定性 | 暂不作为事实；进入人工确认队列。 |
| `block` 拦截 | 凭据、商业敏感信息或无权传递的信息 | 不写入 FDE 交接包，只记录拦截原因。 |
| `discard` 忽略 | 与 FDE 工作无关且不影响范围/风险 | 不写入交接包。 |

### 5.4 必须人工确认的边界

以下信息不得由 Agent 自动发布为 FDE 的既定事实：

1. 对客户作出的上线、功能、价格、资源等承诺；
2. 同一事实存在冲突的日期、范围、负责人或部署方式；
3. 源角色的主观评价或未经证实的客户情绪；
4. 会改变交付范围、风险等级或客户预期的信息。

人工确认是产品能力的一部分：用户应清楚看到 Agent 为什么暂停、需要谁判断、确认后会如何改变目标记忆。

---

## 6. 完整用户旅程

### 6.1 旅程总览

| 阶段 | 用户动作 | Agent / 系统动作 | 用户获得的结果 |
|---|---|---|---|
| 1. 选择交接与上下文 | 选择“澄澈零售：售前 → FDE”场景，勾选上下文来源 | 加载角色卡、资料快照和策略 | 确认本次交接要转换什么，而不是被动处理固定文档。 |
| 2. 理解源上下文 | 浏览已选资料与目标角色规则 | 展示来源类型、资料数量和敏感提示 | 看到售前记忆并不等于可交付事实。 |
| 3. 运行转换 | 点击“生成交接草案” | 抽取、分类、冲突检测、转换、生成目标记忆 | 看到可解释的运行轨迹和条目统计。 |
| 4. 审阅映射 | 点击关键条目 | 显示原文、分类、转换原因、目标字段 | 明确每条内容为何保留、改写或拦截。 |
| 5. 发起方决策 | 处理待确认条目 | 更新目标记忆、审计记录和就绪度 | 防止不确定信息被自动固化。 |
| 6. 目标方检查 | FDE 检查、修改或标记缺失内容 | 记录目标方编辑，并重新组成简报 | 让接收者拥有最终上下文的修订权。 |
| 7. 验证接收方 | 运行预置验证问题 | 基于已检查记忆回答，并展示证据 | 确认 FDE 是否能立即开工。 |
| 8. 发布 | 点击“发布交接包” | 冻结 `v1`、生成审计摘要 | 得到可交付的 FDE Deployment Brief。 |

### 6.2 逐步操作与 Mock 结果

#### 阶段 1：选择场景与上下文

1. 用户进入首页，看到预置场景卡“澄澈零售集团 / AtlasFlow 试点交接”。
2. 卡片显示：售前 → FDE、6 份已选资料、28 条源记忆、交接风险“中”。
3. 点击“配置本次 Context”，打开上下文选择器。默认勾选 6 个资料来源；用户可按 Agent 记忆、知识、离线文件、线上文件快照筛选、取消或恢复勾选。
4. 录屏使用推荐组合后，点击“开始交接”，进入交接工作台。

**验收：** 页面在无网络、无外部账号的条件下立即打开；所有“线上”资料均明确标注为本地同步快照。

#### 阶段 2：查看源资料与目标角色要求

1. 左栏默认展示 `售前 Agent Memory`，用户可切换本次已勾选的 CRM 快照、客户会议纪要、方案承诺、技术问卷、待办、产品知识和传输策略。
2. 用户点击右上角“查看 FDE 接收规则”，看到 FDE 最关心的六类信息：范围、架构/集成、关键人、依赖/风险、验收、首周行动。
3. 页面提示：原始资料中含 3 项商业/安全敏感信息，不能默认迁移。

**体验目标：** 在运行前就让用户意识到“源角色与目标角色的上下文需要翻译”。

#### 阶段 3：生成交接草案

1. 用户点击主按钮“生成 FDE 交接草案”。
2. 中间流程区依次播放五个步骤，每步 300–600ms，可在约 2.5 秒完成：

```text
读取售前资料 → 提取记忆条目 → 识别冲突与敏感项 → 映射 FDE Schema → 生成部署简报
```

3. 最终显示统计：

```text
28 条源记忆
10 条直接迁移
5 条角色化重构
4 条待人工确认
3 条敏感拦截
6 条不相关忽略
```

4. 右栏出现 FDE Deployment Brief 草案，状态为“草案：等待 4 项确认”。

#### 阶段 4：审阅关键转换

用户依次点击三张高价值映射卡：

1. **客户业务目标**  
   源：“客服和门店运营人员处理一次售后/运营问题通常需约 4 小时；客户希望降至 1 小时以内。”  
   转换：`业务目标` → `验收指标候选：问题处理时的信息检索与初步处置 ≤ 1 小时；基线测量方式待发现会确认`。

2. **上线日期承诺**  
   源 A：“销售已向客户表示可在 10 月 6 日上线。”  
   源 B：“客户会议确认 10 月 13 日开始两周试点。”  
   转换：`冲突 / 高影响承诺` → 不写入正式排期，进入人工确认。

3. **技术资料与安全边界**  
   源：“测试环境 `SSO_CLIENT_SECRET=atlas-demo-secret`。”  
   转换：`敏感凭据` → 拦截；FDE 仅看到“SSO 需要客户 IT 提供测试身份与租户配置”。

#### 阶段 5：人工确认

用户在“需要确认”页面完成两个主要动作：

1. 在上线日期冲突卡中选择：`以 10 月 13 日启动两周试点为准；10 月 6 日为售前预估，不作为承诺`。
2. 在“客户 Sponsor 不满意”的主观判断卡中选择：`保留为待验证信号，不写成客户事实`。

系统将更新右栏的 FDE 简报：

- 排期从“计划上线 10 月 6 日”改为“待技术发现会确认；当前参考为 10 月 13 日试点启动”；
- 风险栏新增“Sponsor 参与度待 FDE 在首次发现会验证”。

#### 阶段 6：目标方检查与修改

1. 用户点击“进入 FDE 检查”，页面顶部角色切换为 `目标方：FDE`，右栏简报变为可编辑状态。
2. FDE 检查 `范围与依赖`、`风险`、`首周计划` 三个必读区块；每个区块可点击 `确认`、`标记缺失` 或 `编辑`。
3. 录屏中，FDE 将首条行动修改为“与售前、客户业务 Owner 和待确认的 IT Owner 共同召开技术发现会”，并填写修改理由“需要先确认客户技术接口人”。
4. 系统为该卡打上 `FDE 已编辑` 标签，并在审计记录中保留修改前后 Diff 和理由；被标记缺失的信息会进入 `openQuestions`。
5. 用户点击“完成 FDE 检查”，进入接手验证。

#### 阶段 7：接收方验证

用户点击“运行 FDE 接手检查”。系统不展示自由对话，而是运行四张验证卡：

| 验证问题 | 预期答案 | 证据 |
|---|---|---|
| 客户要达成的业务结果是什么？ | 将门店/售后问题处理时的信息检索与初步处置由约 4 小时缩短至 1 小时以内。 | 客户需求访谈纪要 §2。 |
| 第一个必须确认的技术依赖是什么？ | Azure AD 租户、SSO 协议及 CRM 数据权限模型。 | 技术问卷 Q3/Q7。 |
| FDE 不应向客户承诺什么？ | 具体上线日期与私有化部署方案。 | 承诺审阅记录。 |
| 首周应做什么？ | 开技术发现会、收集数据/权限清单、定义验收基线。 | FDE 简报首周计划。 |

通过后显示：`接手就绪度 86 / 100`，其中缺失项为“客户 IT Owner”和“CRM 字段说明”。

#### 阶段 8：发布

1. 用户点击“发布给 FDE”。
2. 系统显示版本 `FDE Deployment Brief v1.0`，附“4 项人工决策、3 项敏感拦截、4 个验证问题通过”。
3. 用户可点击“导出 Markdown”（P1）或“重新开始”。

---

## 7. 页面与交互设计

### 7.1 信息架构

```text
首页 / Scenario Gallery
└── 交接工作台 / Transfer Workspace
    ├── 上下文选择器 / Context Picker
    ├── 源资料与角色卡
    ├── Agent 运行轨迹与转换映射
    ├── 目标记忆 / Deployment Brief 预览
    └── 审计抽屉
        ├── 人工确认中心 / Review Center
        ├── 目标方检查 / Target Check
        └── 接手验证与发布 / Readiness & Publish
```

推荐使用单页工作台 + 右侧抽屉/全屏步骤页，而不是多层复杂路由。这样录屏中用户的注意力始终集中在“左：源上下文 / 中：Bridge / 右：目标上下文”。

### 7.2 全局视觉规范

- 桌面优先，推荐基准宽度 1440px，高度 900px；最小宽度 1180px；
- 浅色企业工作台，背景浅灰，主体白色卡片；主色为靛蓝/蓝紫；
- 状态色固定：蓝色 `transfer`、紫色 `reframe`、橙色 `review`、红色 `block`、灰色 `discard`；
- 重要风险不得仅靠颜色表达，必须有文字状态标签和图标；
- 所有引用使用“来源文件名 + 段落”样式，可点击定位原文；
- 不使用大段自由生成文本；优先使用卡片、标签、表格和 Checklist；
- 页面顶端永久展示演示模式标签：`Demo scenario · Mock data`。

### 7.3 页面 A：场景首页

**目标：** 用 10 秒解释产品做什么并开始演示。

**布局：**

```text
┌─────────────────────────────────────────────────────────────────┐
│ ContextBridge                           Demo scenario · Mock data│
│ 从任意工作上下文到目标角色可执行的工作记忆                          │
│                                                                 │
│  [ 澄澈零售集团 ]                                               │
│  AtlasFlow 试点交接                                             │
│  售前人员  ───────────────→  FDE                               │
│  6 个已选 Context · 28 条源记忆 · 预计 3 分钟完成                 │
│                                                                 │
│  [配置本次 Context]   [开始交接]   [查看示例输出]                 │
└─────────────────────────────────────────────────────────────────┘
```

**组件与行为：**

| 组件 | 内容 | 行为 |
|---|---|---|
| 产品标题 | ContextBridge | 固定，不可点击。 |
| 副标题 | “从任意工作上下文到目标角色可执行的工作记忆” | 说明通用产品价值。 |
| 场景卡 | 客户、产品、角色、已选 Context 数量、风险等级 | 点击卡片或“开始交接”进入工作台。 |
| 配置本次 Context | 打开上下文选择器 | 展示产品可按实际场景选择来源的能力。 |
| 查看示例输出 | 打开已完成的 FDE 简报预览 | 仅供预览，不改变当前状态。 |

### 7.4 页面 B：交接工作台（核心页面）

**目标：** 同屏展示可选择的源 Context、Agent 的桥接过程和目标角色记忆。该页承担演示约 70% 的时间。

**整体布局：**

```text
┌────────────────────────────────────────────────────────────────────────┐
│ ← 场景   澄澈零售 / AtlasFlow      草案状态：待确认 4 项    [审计 12]  │
├───────────────┬──────────────────────────────┬─────────────────────────┤
│ ① 源：售前    │ ② ContextBridge              │ ③ 目标：FDE            │
│ [6 个 Context]│ [生成 FDE 交接草案]           │ FDE Deployment Brief    │
│               │ ─ 运行步骤/统计 ─            │ [概要][风险][首周计划]  │
│ 原始内容      │ ─ 转换映射卡片 ─              │ 目标记忆卡片             │
│ 证据/标签     │ 原文 → 判断 → 目标字段        │ 接手就绪度 62%           │
└───────────────┴──────────────────────────────┴─────────────────────────┘
```

#### 左栏：源资料与售前记忆（宽度约 30%）

| 功能 | 详细描述 |
|---|---|
| 源角色卡 | 显示“售前人员”、主要关注事项：客户痛点、商机节奏、决策链、方案承诺。 |
| Context 来源条 | 显示 `6 个已选`、来源类型小图标和“管理”按钮；点击打开上下文选择器。 |
| 资料 Tabs | 展示本次已选的 `售前 Agent Memory`、`CRM 快照`、`会议纪要`、`方案承诺`、`技术问卷`、`待办`、`产品知识`。 |
| 原始内容区 | 展示内置 Markdown/JSON 的格式化内容；对可抽取片段显示行号或段落标记。 |
| 记忆标记 | 已完成运行后，对每个片段显示 `事实`、`承诺`、`假设`、`敏感` 等标签。 |
| 证据联动 | 中栏选中某映射卡时，左栏自动定位、黄色高亮对应原文。 |

#### 上下文选择器：Context Picker（抽屉）

**目标：** 让用户看到 ContextBridge 接受的不是固定文档集合，而是可按每次交接需要组合的上下文；本期以本地 Mock 快照实现这种体验，不接入真实外部系统。

**进入方式：** 首页“配置本次 Context”或工作台左栏“管理”。

```text
┌──────────────────────────────────────────────────────────────┐
│ 本次转换的 Context                                  已选 6/8 │
│ [全部] [Agent 记忆] [知识] [离线文件] [线上文件]              │
│                                                              │
│ ☑ Agent 记忆       售前 Copilot Memory              Agent   │
│ ☑ 离线文件         客户需求发现会纪要.md              Local   │
│ ☑ 离线文件         售前方案与承诺.md                  Local   │
│ ☑ 离线文件         技术调研问卷.md                    Local   │
│ ☑ 线上文件快照     CRM 商机摘要（同步于 09:20）        Online  │
│ ☑ 线上文件快照     项目待办（同步于 09:20）            Online  │
│ ☐ 知识             AtlasFlow 产品能力说明              Knowledge│
│ ☐ 离线文件         竞品分析.md                         Local   │
│                                                              │
│ [恢复推荐组合]                               [应用 6 个来源] │
└──────────────────────────────────────────────────────────────┘
```

| 功能 | P0 行为与约束 |
|---|---|
| 来源分类 | 提供 Agent 记忆、知识、离线文件、线上文件四种筛选与图标。 |
| 勾选来源 | 支持勾选/取消勾选 8 个内置 Context；选择会决定左栏 Tabs、源资料计数和参与转换的记忆项。 |
| 线上文件 | 显示“本地同步快照”与固定同步时间；不发生网络访问。 |
| 恢复推荐组合 | 一键恢复录屏使用的 6 个来源，保证演示可复现。 |
| 非推荐来源 | 竞品分析默认不选，体现来源可选择；即使勾选，其商业策略条目仍会被策略拦截。 |
| 应用来源 | 有改动时标记“需重新生成草案”；不自动运行，避免用户无意覆盖审核结果。 |

前端无需实现真实上传或第三方 OAuth；P1 才支持把真实文件接入同一选择器。

#### 中栏：转换流水线与映射（宽度约 40%）

| 功能 | 详细描述 |
|---|---|
| 主按钮 | 未运行前显示“基于 6 个 Context 生成 FDE 交接草案”；运行后显示“重新运行”。 |
| 流水线 | 五个节点：读取、提取、风险检查、角色映射、生成简报。运行时依次变为完成。 |
| 统计条 | 显示 28 / 10 / 5 / 4 / 3 / 6 的分类结果；点击分类可过滤映射列表。 |
| 映射列表 | 每行显示源短句、迁移动作、目标字段、置信度；按高风险优先排序。 |
| 映射详情卡 | 点击行后展开：原始引用、抽取类型、转换理由、目标文本、规则命中、人工状态。 |
| 重点演示按钮 | 可快速聚焦“日期冲突”“敏感凭据”“业务目标重构”三条卡片。 |

映射行示例：

```text
[Review] “10 月 6 日可以上线”  →  排期约束 / 待技术可行性确认      61%
[Reframe] “4 小时定位问题”      →  验收指标候选：≤ 1 小时           92%
[Block]  “SSO_CLIENT_SECRET…”  →  敏感凭据：不迁移                 100%
```

#### 右栏：FDE Deployment Brief（宽度约 30%）

| 功能 | 详细描述 |
|---|---|
| 目标角色卡 | 显示“FDE”；主要关注事项：范围、技术依赖、风险、验收、首周计划。 |
| 简报 Tabs | `概要`、`范围与依赖`、`风险`、`首周计划`。 |
| 草案状态 | 显示“待确认 4 项”或“可发布”。 |
| 内容卡片 | 每张卡片包含目标字段、简洁文本、来源数量和置信状态；目标方检查后显示“FDE 已确认/已编辑/待补齐”。 |
| 就绪度 | 运行后由 62% 升至人工确认后的 86%；点击可查看未满足项。 |
| 目标方检查 | 发起方确认后启用“交给 FDE 检查”；FDE 可编辑目标记忆，再运行接手检查。 |

### 7.5 页面 C：人工确认中心

**目标：** 体现 Agent 在高影响、不确定信息上停下来请求判断。

**进入方式：** 工作台顶部“待确认 4 项”或中栏任一 `Review` 卡。

**布局：** 全屏覆盖层，左侧待处理队列，右侧详情和决策区。

```text
┌──────────────────────┬──────────────────────────────────────────┐
│ 待确认 4 项           │ 上线日期冲突                             │
│ ● 上线日期冲突        │ 原文 A / 原文 B 并排                     │
│ ● Sponsor 参与度      │ Agent 判断：不可自动作为排期事实          │
│ ○ 私有化部署边界      │                                          │
│ ○ 验收指标基线        │ [采用 10/13 试点] [保留为待确认]          │
│                      │ [填写人工备注] [保存决定]                 │
└──────────────────────┴──────────────────────────────────────────┘
```

**四条待确认数据及默认处理：**

| 项目 | 默认状态 | 用户可选动作 | 默认演示选择 |
|---|---|---|---|
| 上线日期冲突 | 必须确认 | 采用 10/13 试点 / 保留待确认 | 采用 10/13 试点。 |
| Sponsor 参与度 | 必须确认 | 转为待验证信号 / 不迁移 | 转为待验证信号。 |
| 内网/私有化范围 | 必须确认 | 定义为硬性约束 / 记录为偏好 | 定义为需技术发现会确认的硬性约束候选。 |
| 成功指标基线 | 必须确认 | 采用候选指标 / 标记待测基线 | 标记待测基线。 |

保存决定后应：

- 更新对应映射卡和目标记忆；
- 在审计抽屉增加一条人工操作；
- 重新计算接手就绪度；
- 将“人工确认”徽标写入目标记忆卡。

### 7.6 页面 D：目标方检查、接手验证与发布

**目标：** 确保目标角色可以修订转换结果，再将“转换得看起来不错”变为“FDE 能用它开工”的证据。

**布局：** 采用两步 Tab：`① FDE 检查` 与 `② 接手验证与发布`。左侧始终展示 `FDE Deployment Brief`，右侧依当前步骤展示编辑区或验证卡。

| 组件 | 行为 |
|---|---|
| FDE 检查清单 | `范围与依赖`、`风险`、`首周计划` 必须逐项点击确认或标记缺失。 |
| 目标记忆编辑 | 每张 Brief 卡提供 `编辑`；点击后变为多行文本框，支持保存、取消、恢复 Agent 草案。保存时必须填写不超过 80 字的修改理由。 |
| 标记缺失 | FDE 可将一条内容标为“需要补齐”，系统把它加入 `openQuestions`，不会从简报中静默删除。 |
| 编辑 Diff | 显示 Agent 原文、FDE 修改后文本和理由；已编辑卡显示 `FDE 已编辑` 标签。 |
| 运行接手检查 | 依次播放四个检查；每题约 300ms 后展示答案和来源。 |
| 验证卡 | 显示问题、答案、状态“通过/需补齐”、引用来源。 |
| 就绪度仪表盘 | 显示 86/100；列出缺失的 IT Owner、CRM 字段说明。 |
| 发布按钮 | FDE 完成检查且验证通过后写入版本 `v1.0`，显示成功 toast。 |
| 导出（P1） | 下载 `fDE-deployment-brief.md`。 |

### 7.7 审计抽屉

在工作台右上角显示 `审计 12`。点击打开侧边栏，按时间列出：

- Agent 运行开始/结束；
- 每个规则判断的摘要；
- 三项敏感信息拦截；
- 每项人工决策；
- 接手检查结果；
- 发布事件。

每个事件包含时间、执行主体（Agent / 人工）、对象、动作和简短理由。Demo 中使用固定相对时间，如“刚刚”“1 分钟前”，避免真实时间变化影响录屏。

---

## 8. Mock 数据设计

### 8.1 数据原则

1. 所有公司、人员、凭据和业务信息为虚构；
2. 数据必须同时包含已确认事实、模糊表述、冲突、承诺、主观判断和敏感信息；
3. 每一类转换动作至少有 2 个可展示案例；
4. 所有关键输出有确定的“期望答案”，便于演示和后续测试；
5. Mock 数据是产品体验的一部分，不能只有干净正确的资料。

### 8.2 建议目录

```text
mock/
  scenario.json
  context_sources.json
  roles/
    sales_role_card.md
    fde_role_card.md
  knowledge/
    atlasflow_capabilities.md
  source/
    crm_opportunity.json
    sales_memory.md
    discovery_meeting.md
    solution_commitments.md
    technical_questionnaire.md
    open_tasks.csv
    competitor_notes.md
  policy/
    transfer_policy.json
  expected/
    memory_items.json
    target_brief.json
    target_check_edits.json
    validation_cases.json
    audit_events.json
```

### 8.3 Context 来源清单：`context_sources.json`

该文件驱动 Context Picker。所有 `online_snapshot` 都是内置 JSON/Markdown 快照，不需要联网；`selectedByDefault` 是录屏推荐组合。

```json
[
  {"id":"CTX-01","name":"售前 Copilot Memory","type":"agent_memory","path":"source/sales_memory.md","selectedByDefault":true},
  {"id":"CTX-02","name":"客户需求发现会纪要","type":"local_file","path":"source/discovery_meeting.md","selectedByDefault":true},
  {"id":"CTX-03","name":"售前方案与承诺","type":"local_file","path":"source/solution_commitments.md","selectedByDefault":true},
  {"id":"CTX-04","name":"技术调研问卷","type":"local_file","path":"source/technical_questionnaire.md","selectedByDefault":true},
  {"id":"CTX-05","name":"CRM 商机摘要","type":"online_snapshot","path":"source/crm_opportunity.json","syncedAt":"09:20","selectedByDefault":true},
  {"id":"CTX-06","name":"项目待办","type":"online_snapshot","path":"source/open_tasks.csv","syncedAt":"09:20","selectedByDefault":true},
  {"id":"CTX-07","name":"AtlasFlow 产品能力说明","type":"knowledge","path":"knowledge/atlasflow_capabilities.md","selectedByDefault":false},
  {"id":"CTX-08","name":"竞品分析笔记","type":"local_file","path":"source/competitor_notes.md","selectedByDefault":false}
]
```

### 8.4 场景元数据：`scenario.json`

```json
{
  "id": "chengche-atlasflow-2026",
  "title": "澄澈零售集团 · AtlasFlow 试点交接",
  "customer": "澄澈零售集团",
  "product": "AtlasFlow 企业知识与流程智能助手",
  "sourceRole": "售前人员",
  "targetRole": "FDE",
  "sourceDocumentCount": 6,
  "sourceMemoryCount": 28,
  "riskLevel": "medium",
  "description": "售前完成需求与方案沟通后，选择客户上下文交接给 FDE 启动试点。"
}
```

### 8.5 源材料内容

#### `crm_opportunity.json`

```json
{
  "account": "澄澈零售集团",
  "opportunity": "客服与门店运营智能助手试点",
  "pilotUsers": 300,
  "sites": ["华东区域", "华南区域"],
  "economicBuyer": "王敏 / 客服运营总监",
  "businessChampion": "陈锐 / 门店运营经理",
  "technicalContact": "待客户确认",
  "salesStage": "合同评审中",
  "commercialNotes": "折扣底线 18%，竞品为 ExampleAssist"
}
```

`commercialNotes` 必须被标记为敏感商业信息，不进入 FDE 输出。

#### `sales_memory.md`

```md
# 售前工作记忆（林远）

- 客户最想解决的是客服和门店运营人员跨运营手册、历史售后工单查找处理方法太慢；陈锐说一次通常约 4 小时，希望缩短到 1 小时以内。
- 王敏很关注风险，偏好每周三收到一页纸更新；她是最终预算审批人。
- 客户希望首批覆盖华东和华南区域约 300 名客服与门店运营用户。
- 我在会上说过 AtlasFlow 支持 Azure AD SSO 和 Salesforce 服务工单连接；具体租户、协议和字段尚未拿到。
- 客户“可能”只接受私有化/内网方案，安全团队意见仍待确认。
- 陈锐上次会后回复较慢，我感觉他对试点价值没有完全信心。（个人判断）
- 内部预测：若客户接受标准合同，预计签约概率约 80%。（商业敏感）
- 测试环境记录：SSO_CLIENT_SECRET=atlas-demo-secret
```

#### `discovery_meeting.md`

```md
# 2026-09-14 客户需求发现会纪要

## 已确认
- 试点以“门店运营与售后问题处理”场景为起点，不在第一阶段接入库存和收银系统。
- 客户愿意先接入门店运营手册和 Salesforce 服务工单。
- 客户期望由 Azure AD 管理员工身份。

## 客户目标
- 将一次门店/售后问题的信息检索和初步处置从约 4 小时缩短至 1 小时以内。

## 时间讨论
- 客户希望 2026-10-13 启动两周试点；正式投产日期需要在技术发现会后共同确认。

## 待客户补充
- Azure AD 租户、SSO 协议；
- CRM 字段说明、接口权限；
- 客户 IT Owner；
- 运营手册文档量与更新频率。
```

#### `solution_commitments.md`

```md
# 售前方案与沟通承诺

- 方案包含 AtlasFlow 检索、问答、工单摘要能力。
- 销售邮件中曾写：“若客户资料准备完毕，我们预计可于 2026-10-06 上线。”
- 客户提出希望在内网环境运行；售前方案中描述为“可评估私有化部署路径”，并非已签署能力承诺。
- 售前承诺在项目启动后协助安排每周风险更新。
```

#### `technical_questionnaire.md`

```md
# 技术调研问卷（未完整）

| 项目 | 当前信息 | 状态 |
|---|---|---|
| 身份认证 | Azure AD | 协议、租户 ID、测试账号缺失 |
| 知识库 | 门店运营手册 | 文档量、格式、更新频率缺失 |
| CRM | Salesforce 服务工单 | 字段、对象、API Scope 缺失 |
| 部署 | 偏好内网 | 网络、算力、运维责任缺失 |
| 试点用户 | 300 人 | 用户分组和培训计划缺失 |
```

#### `open_tasks.csv`

```csv
id,title,owner,due_date,status
T-101,客户确认 Azure AD 技术联系人,客户 IT Owner,2026-09-20,blocked
T-102,收集 Salesforce 字段字典,未分配,2026-09-23,open
T-103,安排项目启动与风险周报节奏,林远,2026-09-18,open
T-104,明确试点验收基线,陈锐,2026-09-25,open
```

#### `atlasflow_capabilities.md`（可选择的知识来源）

```md
# AtlasFlow 产品能力说明

- 支持连接文档型知识库与 Salesforce 服务工单；具体连接器启用需确认 API Scope。
- 支持 Azure AD SSO；需由客户提供租户、协议和测试身份。
- 可评估内网部署路径；网络、算力、运维责任和上线排期不属于默认承诺。
```

该来源在录屏中默认不勾选，但 Context Picker 中可见；勾选后可为“产品能力”类记忆提供额外证据，不能自动消除环境/权限等交付风险。

#### `competitor_notes.md`（默认不选）

```md
# 竞品分析（商业敏感）

- 客户同时在比较 ExampleAssist。
- 当前折扣底线为 18%。
```

即使用户勾选该 Context，内容也因商业敏感规则被拦截，不会进入 FDE Brief。

### 8.6 目标角色规则：`fde_role_card.md`

```md
# FDE 角色接手规则

FDE 的交接记忆必须优先回答：
1. 客户想达成什么结果，怎样验收？
2. 首阶段已确认范围和明确排除项是什么？
3. 架构、身份、数据、环境、权限依赖分别是什么？
4. 谁负责业务、技术和验收？
5. 哪些承诺、风险或假设需要在技术发现会澄清？
6. FDE 首个工作周应该执行哪些动作？

不应接收：折扣、竞争策略、未经证实的个人评价、凭据、与交付无关的商机预测。
```

### 8.7 转换规则：`transfer_policy.json`

```json
{
  "rules": [
    {
      "id": "P-01",
      "when": "contains_secret_or_credential",
      "action": "block",
      "reason": "安全凭据不得写入目标记忆"
    },
    {
      "id": "P-02",
      "when": "commercial_strategy_or_discount",
      "action": "block",
      "reason": "商业敏感信息与 FDE 交付无关"
    },
    {
      "id": "P-03",
      "when": "external_commitment_or_date_without_feasibility",
      "action": "review",
      "reason": "客户承诺必须由交付负责人确认"
    },
    {
      "id": "P-04",
      "when": "subjective_or_unverified_assessment",
      "action": "review",
      "reason": "主观判断不能自动固化为客户事实"
    },
    {
      "id": "P-05",
      "when": "business_need_or_customer_preference",
      "action": "reframe",
      "reason": "转写为 FDE 的验收、工作方式或风险项"
    }
  ]
}
```

### 8.8 期望抽取与转换结果：`memory_items.json`

前端不需要在浏览器端真正完成自然语言抽取；P0 可加载以下结构化结果作为 Demo 场景的运行产物。每条仍需在界面清楚地展示其证据和判断。

```json
[
  {
    "id": "M-01",
    "source": "discovery_meeting.md#客户目标",
    "quote": "将一次门店/售后问题的信息检索和初步处置从约 4 小时缩短至 1 小时以内。",
    "type": "customer_goal",
    "reliability": "confirmed",
    "sensitivity": "normal",
    "action": "reframe",
    "targetField": "acceptance_candidate",
    "targetText": "验收指标候选：将门店/售后问题处理时的信息检索与初步处置从约 4 小时缩短至 1 小时以内；基线测量方式待发现会确认。",
    "confidence": 92,
    "ruleId": "P-05"
  },
  {
    "id": "M-02",
    "source": "sales_memory.md#王敏",
    "quote": "王敏很关注风险，偏好每周三收到一页纸更新；她是最终预算审批人。",
    "type": "stakeholder_preference",
    "reliability": "confirmed",
    "sensitivity": "normal",
    "action": "reframe",
    "targetField": "stakeholder_map",
    "targetText": "王敏（客服运营总监 / 预算审批人）：关注项目风险；建议沿用每周三一页纸风险更新。",
    "confidence": 89,
    "ruleId": "P-05"
  },
  {
    "id": "M-03",
    "source": "solution_commitments.md#上线时间",
    "quote": "若客户资料准备完毕，我们预计可于 2026-10-06 上线。",
    "type": "external_commitment",
    "reliability": "unverified",
    "sensitivity": "normal",
    "action": "review",
    "targetField": "timeline_constraint",
    "targetText": "售前曾提出 10/06 上线预估；与客户会议中的 10/13 试点启动存在冲突，需交付负责人确认。",
    "confidence": 61,
    "ruleId": "P-03"
  },
  {
    "id": "M-04",
    "source": "sales_memory.md#主观判断",
    "quote": "我感觉他对试点价值没有完全信心。",
    "type": "subjective_assessment",
    "reliability": "opinion",
    "sensitivity": "internal",
    "action": "review",
    "targetField": "stakeholder_risk",
    "targetText": "待验证信号：业务 Champion 的试点价值认同度需要在首次发现会确认。",
    "confidence": 47,
    "ruleId": "P-04"
  },
  {
    "id": "M-05",
    "source": "sales_memory.md#测试环境",
    "quote": "SSO_CLIENT_SECRET=atlas-demo-secret",
    "type": "credential",
    "reliability": "confirmed",
    "sensitivity": "secret",
    "action": "block",
    "targetField": null,
    "targetText": "已拦截：不迁移任何凭据；FDE 需向客户 IT 索取测试身份与租户配置。",
    "confidence": 100,
    "ruleId": "P-01"
  },
  {
    "id": "M-06",
    "source": "crm_opportunity.json#commercialNotes",
    "quote": "折扣底线 18%，竞品为 ExampleAssist",
    "type": "commercial_strategy",
    "reliability": "confirmed",
    "sensitivity": "commercial",
    "action": "block",
    "targetField": null,
    "targetText": "已拦截：折扣与竞品策略不进入 FDE 交接包。",
    "confidence": 100,
    "ruleId": "P-02"
  }
]
```

其余结果可按同一 Schema 补足至 28 条；页面统计应与 10/5/4/3/6 的分类数一致。

### 8.9 期望目标输出：`target_brief.json`

```json
{
  "version": "draft",
  "accountSummary": "澄澈零售集团将以门店运营与售后问题处理场景启动 AtlasFlow 试点，覆盖华东、华南区域约 300 名客服与门店运营用户。",
  "businessOutcome": "将门店/售后问题处理时的信息检索与初步处置从约 4 小时缩短至 1 小时以内；基线测量方式待确认。",
  "confirmedScope": [
    "门店运营手册接入",
    "Salesforce 服务工单接入",
    "Azure AD 身份管理",
    "首批 300 人试点"
  ],
  "excludedScope": ["第一阶段不接入库存和收银系统"],
  "stakeholders": [
    "王敏：客服运营总监、预算审批人、偏好每周三风险更新",
    "陈锐：门店运营经理、业务 Champion；价值认同度待确认",
    "客户 IT Owner：待确认"
  ],
  "dependencies": [
    "Azure AD 租户、协议与测试身份",
    "Salesforce 字段字典、对象与 API Scope",
    "门店运营手册文档量、格式与更新频率",
    "内网部署的网络、算力和运维边界"
  ],
  "risks": [
    "10/06 上线预估与 10/13 试点启动信息冲突，未形成客户承诺",
    "私有化/内网是否为硬性约束待技术发现会确认",
    "试点验收基线和用户分组待补齐"
  ],
  "firstWeekPlan": [
    "召开技术发现会，确认 Azure AD、CRM 和部署约束",
    "建立客户 IT Owner、业务 Owner 与 FDE 的责任人表",
    "收集数据/权限清单并评估连接器可行性",
    "与客户共同定义试点验收基线及用户分组",
    "确认每周三风险更新节奏"
  ],
  "readinessScore": 86,
  "openQuestions": ["客户 IT Owner", "Salesforce 字段说明与 API Scope"]
}
```

### 8.10 目标方检查 Mock：`target_check_edits.json`

这份数据保证“目标用户可以检查和修改”不是只停留在页面描述。FDE 检查时默认演示一条编辑、一条确认和一条缺失标记。

```json
[
  {
    "id": "EDIT-01",
    "briefField": "firstWeekPlan[0]",
    "originalText": "召开技术发现会，确认 Azure AD、CRM 和部署约束",
    "editedText": "与售前、客户业务 Owner 及待确认的 IT Owner 共同召开技术发现会，确认 Azure AD、CRM 和部署约束",
    "reason": "需要先明确客户技术接口人和三方职责",
    "status": "edited",
    "editedBy": "FDE"
  },
  {
    "id": "CHECK-01",
    "briefField": "dependencies",
    "status": "confirmed",
    "editedBy": "FDE"
  },
  {
    "id": "CHECK-02",
    "briefField": "openQuestions",
    "addedQuestion": "客户需确认门店运营手册中是否含有受限员工信息",
    "status": "needs_info",
    "editedBy": "FDE"
  }
]
```

---

## 9. 功能规格（Spec）

本节是后续技术方案、前端状态管理、接口契约和测试设计的依据。

### 9.1 核心领域对象

#### `TransferSession`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string | 本次交接会话 ID。 |
| `scenarioId` | string | POC 固定为 `chengche-atlasflow-2026`；模型不限制其他场景。 |
| `sourceRole` / `targetRole` | RoleProfile | POC 为 `sales` / `fde`；Role Profile 可配置。 |
| `selectedContextIds` | string[] | 本次参与转换的 Context 来源 ID；默认 6 个。 |
| `status` | enum | `not_started`、`running`、`draft`、`in_review`、`target_check`、`ready_for_validation`、`published`。 |
| `memoryItems` | MemoryItem[] | 抽取并处理后的源记忆。 |
| `decisions` | ReviewDecision[] | 人工确认的决定。 |
| `targetBrief` | TargetBrief | 当前目标交接包。 |
| `readiness` | ReadinessResult | 接手检查结果。 |
| `auditEvents` | AuditEvent[] | 审计事件。 |

#### `ContextSource`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` / `name` | string | 如 `CTX-01` / `售前 Copilot Memory`。 |
| `type` | enum | `agent_memory`、`knowledge`、`local_file`、`online_snapshot`。 |
| `path` | string | POC 内置 Mock 文件路径。 |
| `selected` | boolean | 是否参与本次转换。 |
| `syncedAt` | string/null | 线上文件快照显示同步时间；不代表实时连接。 |

#### `MemoryItem`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string | 如 `M-03`。 |
| `source` / `quote` | string | 原始证据定位与原文。 |
| `type` | enum | `fact`、`customer_goal`、`requirement`、`commitment`、`risk`、`opinion`、`credential` 等。 |
| `reliability` | enum | `confirmed`、`unverified`、`opinion`、`conflicted`。 |
| `sensitivity` | enum | `normal`、`internal`、`commercial`、`secret`。 |
| `action` | enum | `transfer`、`reframe`、`review`、`block`、`discard`。 |
| `targetField` | string/null | 如 `dependencies`、`timeline_constraint`。 |
| `targetText` | string | 在目标角色工作语言中表达的结果。 |
| `confidence` | number | 0–100。 |
| `ruleId` | string | 命中的策略 ID。 |
| `reviewState` | enum | `pending`、`resolved`、`not_required`。 |

#### `ReviewDecision`

| 字段 | 类型 | 说明 |
|---|---|---|
| `memoryItemId` | string | 被决定的 `MemoryItem`。 |
| `decision` | enum | `accept_as_constraint`、`keep_open`、`convert_to_signal`、`discard`。 |
| `note` | string | 人工备注。 |
| `decidedBy` | string | POC 固定为“交付负责人”。 |
| `timestamp` | string | Demo 使用固定/相对时间。 |

#### `TargetBriefItemEdit`

| 字段 | 类型 | 说明 |
|---|---|---|
| `briefField` | string | 被修改的目标记忆字段。 |
| `originalText` / `editedText` | string | Agent 草案与目标方最终文字。 |
| `reason` | string | 目标方修改原因，1–80 字，必填。 |
| `status` | enum | `confirmed`、`edited`、`needs_info`。 |
| `editedBy` | string | POC 固定为 `FDE`。 |

### 9.2 状态机

```text
not_started
  └─[运行转换]→ running
       └─[转换完成]→ draft
            ├─[存在 review 项]→ in_review
            │    └─[所有必需项处理]→ target_check
            └─[无 review 项]→ target_check
                 └─[FDE 检查完成]→ ready_for_validation
                      └─[接手检查通过]→ published

任意非 published 状态 ─[重置演示]→ not_started
```

**发布前置条件：**

- `M-03` 上线日期冲突已处理；
- `M-04` Sponsor 主观判断已处理；
- FDE 已检查 `范围与依赖`、`风险`、`首周计划` 三个区块；
- 运行四项接手检查；
- `readinessScore >= 80`。

### 9.3 Agent 工作流契约

POC 页面需将以下阶段呈现为 Agent 工作流。技术实现可使用真实 LLM、Mock 运行产物或两者可切换，但 UI 语义不得改变。

| 阶段 | 输入 | 输出 | 规则 |
|---|---|---|---|
| `ingest` | 用户勾选的 Context 来源、两个角色卡 | 来源清单、原始片段 | 不改变源资料；线上文件只读取内置快照。 |
| `extract` | 原始片段 | `MemoryItem[]` 初稿 | 每个条目必须保留 `source` 和 `quote`。 |
| `govern` | MemoryItem、Transfer Policy | action、敏感等级、冲突标记 | 凭据/商业敏感项优先拦截。 |
| `transform` | 合法项、FDE Role Card | FDE Schema 下的目标条目 | 业务目标要转为验收候选；承诺要转为约束/风险。 |
| `compose` | 已处理条目、人工决定 | TargetBrief | 不能将 `pending review` 的内容写成已确认事实。 |
| `target_check` | TargetBrief、FDE 编辑/缺失标记 | 修订版 TargetBrief、编辑 Diff | 目标方可以修改表达或标记缺失，不能解除 `block`。 |
| `validate` | TargetBrief、Validation Cases | 回答、引用、readiness | 所有答案必须能引用目标记忆中的依据。 |

### 9.4 规则优先级

当一条文本同时匹配多条规则时，依序采用：

```text
secret / credential block
> commercial sensitive block
> conflicted or high-impact commitment review
> subjective judgement review
> role-based reframe
> direct transfer
> discard
```

### 9.5 就绪度计算

POC 使用透明、可复现的规则评分，不要求由 LLM 自由计算。

| 维度 | 满分 | 演示结果 | 判定 |
|---|---:|---:|---|
| 客户目标与范围 | 20 | 20 | 已确认。 |
| 关键人与协作节奏 | 15 | 12 | IT Owner 缺失。 |
| 架构/数据/权限依赖 | 25 | 18 | 多项技术信息待补。 |
| 风险与承诺治理 | 20 | 18 | 日期已人工处理，私有化待确认。 |
| 首周计划与待办 | 10 | 10 | 已生成。 |
| 信息安全与来源可追溯 | 10 | 8 | 敏感项已拦截，部分材料仍不完整。 |
| **总计** | **100** | **86** | 可启动技术发现会，不等于可承诺上线。 |

### 9.6 前端交互要求

1. “运行”后使用固定时长动画，但不得阻塞用户超过 3 秒；
2. 已选 Context、筛选、人工决定、FDE 编辑和发布状态应在单次刷新期间保留；可选地使用浏览器本地存储；
3. 用户切换映射卡时，左栏原文高亮、右栏目标字段高亮；
4. `block` 项始终不能通过 UI 改为迁移；
5. 用户改变 Context 组合后，当前草案必须标记为“需要重新生成”；发布按钮在不满足前置条件时禁用，并显示明确原因；
6. “重新开始”必须恢复为固定 Mock 初始状态；
7. Demo 模式必须可在无网络条件下完整完成，但应清楚标记为 `Mock data / Demo mode`；
8. 如果提供真实模型模式，必须与 Demo 模式区分展示，不能将固定产物伪装为实时模型推理；
9. FDE 编辑必须显示修改前后 Diff 和理由，且更新审计事件。

### 9.7 非功能要求

| 维度 | 要求 |
|---|---|
| 可部署性 | 本地单机运行；README 提供安装、启动、测试和重置说明。 |
| 稳定性 | 固定样例在无网络下 100% 可复现；无随机输出影响录屏。 |
| 性能 | 首页加载 < 2 秒；点击运行到出现结果 < 3 秒。 |
| 可访问性 | 状态不只依赖颜色；按钮、Tabs、弹窗具备键盘可操作基础。 |
| 数据安全演示 | 所有数据虚构；凭据仅为显式 Mock 字符串；不请求真实业务凭据。 |
| 可测试性 | Mock 输出、人工决策、发布条件和就绪度计算均可单元测试。 |

---

## 10. 验收标准与测试场景

### 10.1 核心验收

| 编号 | 场景 | 期望结果 |
|---|---|---|
| AC-01 | 进入首页并启动场景 | 能看到通用“跨角色 Context 转换”定位，以及售前 → FDE 的固定演示数据。 |
| AC-02 | 打开 Context Picker | 能看到 Agent 记忆、知识、离线文件、线上文件快照四种来源及默认 6/8 的选择状态。 |
| AC-03 | 点击生成交接草案 | 显示五阶段运行过程和 28 条分类统计。 |
| AC-04 | 查看业务目标映射 | 能看到原文、重构后的验收候选及来源。 |
| AC-05 | 查看日期冲突 | 能看到两份冲突原文，且该信息默认不进入正式排期。 |
| AC-06 | 查看测试凭据 | 能看到已拦截理由；FDE 目标包不出现 secret 原文。 |
| AC-07 | 完成两个演示人工决策 | 目标简报、审计记录和就绪度发生预期更新。 |
| AC-08 | FDE 编辑首周计划 | 可修改目标 Brief、填写理由、看到 Diff 和 `FDE 已编辑` 标签。 |
| AC-09 | 运行接手检查 | 四题答案、引用和 86 分就绪度正确显示。 |
| AC-10 | 尝试提前发布 | 按钮禁用且提示未完成 FDE 检查或验证。 |
| AC-11 | 发布后重置 | 可回到固定的初始 Mock 状态并再次运行。 |

### 10.2 容易出错的核心检查

1. **把售前承诺误写为技术已确认事实**：`M-03` 必须保持 `review`，直到人工决定；
2. **把源角色主观判断误写为客户事实**：`M-04` 的默认结果只能是“待验证信号”；
3. **敏感信息泄露**：`SSO_CLIENT_SECRET` 和折扣信息不应出现在任何 TargetBrief、验证答案、搜索结果或导出内容中；
4. **错误来源归因**：每张目标记忆卡都能定位到一个或多个源文件与片段；
5. **角色转换退化为摘要**：业务目标、部署偏好、时间承诺必须分别转换为验收指标、架构约束和排期风险，而不是原句复制。

---

## 11. 3 分钟演示建议

| 时间 | 操作 | 要传达的产品信息 |
|---:|---|---|
| 0:00–0:15 | 首页展示“任意角色 → 任意角色”的定位，启动“澄澈零售：售前 → FDE”案例 | 售前 → FDE 是示例，产品能力是跨角色 Context 转换。 |
| 0:15–0:32 | 打开 Context Picker，展示 4 类来源与默认 6/8 组合 | Context 可按需选择，不是固定文档处理。 |
| 0:32–0:52 | 浏览源资料和 FDE 角色规则 | 源资料混有事实、承诺、观点和敏感信息。 |
| 0:52–1:15 | 运行转换 | Agent 有明确的提取、治理、转换流程。 |
| 1:15–1:40 | 查看业务目标重构、日期冲突、敏感拦截 | 展现“Bridge”与安全边界。 |
| 1:40–2:02 | 在确认中心处理两条风险信息 | 高影响判断保留人工。 |
| 2:02–2:25 | FDE 检查并编辑首周计划 | 目标用户拥有检查和修订转换结果的权利。 |
| 2:25–3:00 | 运行接手检查并发布 | 用可验证性而不是“看起来像总结”证明价值。 |

---

## 12. 面试交付中需要自然覆盖的产品判断

这一节服务于题目要求，但不应主导演示；相关能力已经被嵌入产品流程。

### 12.1 人工保留点

系统将“上线日期、私有化部署范围、客户承诺、主观客户判断”等高影响且不确定的信息送入确认中心，不允许 Agent 自动发布。原因是这些内容会影响范围、交付成本、客户预期和组织责任；Agent 应负责把证据、冲突和选择讲清楚，而非替代交付负责人承担承诺。

### 12.2 最易错点与验证

最易错的是把售前的自然语言承诺、推测或偏好误判为正式、可执行的 FDE 事实。产品以结构化类型、来源引用、规则优先级、冲突卡和接收方四问来验证；同时以测试用例确保 secret、折扣和主观判断不会泄漏或被错误固化。

### 12.3 “下周上线”的生产缺口

最难补齐的不是前端或 Prompt，而是可信、实时且有责任人的交付数据与组织流程：真实 CRM 信息、客户 IT Owner、身份/数据/网络环境、权限、验收基线、资源排期、数据脱敏、SSO、审计、异常回退和评测集。POC 通过本地 Mock 数据证明工作流与体验；生产上线必须先把这些权威数据源、责任边界和治理能力补齐。

---

## 13. 后续技术方案必须回答的事项

后续技术方案至少应逐项说明：

1. 前端框架、页面路由、组件分层、状态管理和动画实现；
2. Mock 数据的加载方式、领域模型和本地存储策略；
3. Demo 模式、真实模型模式和两者的清晰切换；
4. Agent 工作流如何实现 `ingest → extract → govern → transform → compose → validate`；
5. 敏感项检测、冲突检测、规则优先级和人工审核状态机；
6. 交接简报、验证答案、就绪度和审计事件的生成方式；
7. 单元测试、端到端测试和敏感信息泄露测试；
8. 本地安装、运行、重置和录屏准备步骤；
9. 若扩展到生产，如何处理身份、权限、数据源、审计、模型成本和观测性。
