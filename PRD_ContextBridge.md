# ContextBridge POC PRD（v3）

> **v3 收敛说明（本说明优先于下文旧示意）**：本次 Demo 只使用三份中文 Mock 文件：两份来自 `Codex` Mock 记忆路径的记忆文件，以及一份通过本地文件选择器添加的知识文件。记忆不再由用户从电脑上传；用户先选择 Agent（录屏选择 Codex），再在 `~/.codex/agents/sales/memory/` 的 Mock 路径中勾选记忆文件。知识文件仍可从电脑选择。所有早期“三份知识、五份文件、选择本地记忆文件”的描述均由本说明替代。

## 1. 文档目的

ContextBridge 是一个跨角色上下文转换产品：用户先自行配置一次交接任务，再将源角色的本地 Agent 记忆和本地知识资料，转换为目标角色可检查、可修改、可执行的工作记忆。

本次 POC 用“售前人员 → FDE（客户交付工程师）”演示最强的语言与职责反差，但不把产品模型绑定到这对角色。

本版优先级：

1. 让观看者在 30 秒内看懂“源角色的混乱工作语言”如何变成“目标角色的行动语言”；
2. 让任务、角色、输入文件、转换要求都可配置；
3. 确保全部能力可在本地、无 API Key 的条件下稳定录屏；
4. 用少量但真实、有冲突的 Mock 文件证明这不是文档摘要。

## 2. 产品定位

> **ContextBridge 不是把文件从 A 复制到 B，而是把 A 的工作记忆按 B 的职责、风险边界和下一步动作重新组织。**

源角色习惯记录关系、机会、口头承诺、感受和碎片待办；目标角色需要范围、依赖、责任人、风险和启动计划。产品的价值在于保留有用证据、改变表达方式、暴露缺口，并阻止不该迁移的内容。

## 3. P0 范围

### 3.1 必须实现

- 可配置源角色和目标角色；
- 可选择 Agent，并从其记忆存储路径选择输入；录屏用 `Codex / ~/.codex/agents/sales/memory/` Mock 目录；
- 可从本地电脑上传知识文件；
- 可输入本次转换要求 Prompt；
- 配置完成后生成一张可编辑的交接任务卡；只有从任务卡点击“开始交接”才进入工作台；
- 极简三段式对照：源角色原始记忆 → Bridge 决策 → 目标角色工作记忆；
- 对外承诺、主观判断、部署约束、验收缺口必须保留人工确认；
- 目标角色可以检查、编辑并带理由发布结果；
- 提供真实可选择的本地 Mock 文件，且默认 Demo Replay 无需模型 Key。

### 3.2 本期不做

- 在线 CRM、网盘、知识库、项目管理工具连接；
- 文件持久化、多人协作、登录与权限；
- 通用文件解析平台；支持 `.md`、`.txt`、`.json` 已足够；
- 真实模型输出作为录屏主路径。

## 4. 用户、角色与示例

### 4.1 演示操作者

售前负责人、交付负责人或企业客户的试用人员。操作者配置一次交接并观看/参与转换；最终接收方为目标角色。

### 4.2 可配置 Role Profile

| 类型 | 可选角色 | 工作语言 / 重点 |
|---|---|---|
| 源角色 | 售前人员、客户成功经理、一线支持负责人 | 客户诉求、关系、承诺、运营信号、零散待办 |
| 目标角色 | FDE、运维工程师、客户成功经理 | 范围、依赖、风险、责任人、首周动作 |

POC 为每个角色展示职责说明；完整且经过设计的转换样例为“售前人员 → FDE”。其他组合可完成任务配置与通用转换，但不承诺行业语义覆盖。

### 4.3 Demo 场景

客户为虚构的“澄澈零售集团”。售前正把 AtlasFlow 智能助手试点交给 FDE。

- 业务痛点：门店运营与售后人员从运营手册和历史工单中找处理方法约需 4 小时；客户希望缩短至 1 小时内；
- 已讨论范围：门店运营手册、Salesforce 服务工单、Azure AD；第一阶段不接库存和收银；
- 明确冲突：售前邮件写过“10/06 预计上线”，客户会议中实际讨论的是 10/13 启动两周试点；
- 明确缺口：Azure AD 租户/协议/测试身份，Salesforce API Scope，内网网络/算力，IT Owner，试点用户分组；
- 敏感噪声：测试凭据、折扣底线、竞品信息。

## 5. 核心用户旅程

| 阶段 | 用户动作 | 产品响应 | 用户应感受到的价值 |
|---|---|---|---|
| 1. 配置 | 选择源/目标角色、选择 Agent 和记忆路径文件、添加知识文件、输入 Prompt | 校验配置完整性 | 这是一次可定义的交接任务，不是固定 Demo |
| 2. 生成任务 | 点击“生成交接任务卡” | 卡片总结角色、文件数、Prompt 和风险提示 | 我知道本次 Agent 要做什么、将使用什么输入 |
| 3. 开始交接 | 点击卡片“开始交接” | 进入对照工作台 | 任务明确后才开始运行 |
| 4. 阅读源记忆 | 查看售前原话和本地文件来源 | 高亮口头承诺、感受、商业敏感、未补齐信息 | 源资料天然混乱且偏售前视角 |
| 5. Bridge | 运行转换并查看每条决策 | 直接迁移、角色化改写、人工确认、拦截 | Agent 在做受控翻译，不是在总结 |
| 6. 目标检查 | FDE 确认区块、编辑条目、写理由 | 保存 Diff 与审计记录 | 接收方对自己记忆拥有最终控制权 |
| 7. 发布 | 完成验证并发布 | 展示接手问题与首周计划 | 可启动发现会，但不伪造“已经能上线” |

### 5.1 三分钟录屏路径

| 时间 | 演示动作 | 核心信息 |
|---|---|---|
| 0:00–0:25 | 在配置器选择“售前人员 → FDE”，选择 Codex 并展开其 Mock 记忆路径 | Agent 记忆不是普通本地文件上传 |
| 0:25–0:42 | 勾选两份记忆、添加一份知识文件，输入要求 Prompt，生成任务卡 | 仅三份短中文文件，差异清晰 |
| 0:42–0:55 | 点击“开始交接” | 明确执行边界 |
| 0:55–1:35 | 左侧读售前的混乱笔记；中间点开“10/06 上线”“我感觉 Champion 没信心”“client secret” | 销售语言不等于交付事实，敏感内容被阻断 |
| 1:35–2:05 | 在右侧展示 FDE Brief：范围、技术依赖、风险、首周计划 | 同一证据被翻译为交付语言 |
| 2:05–2:30 | 处理 3 项人工确认（上线日期、内网部署、主观判断） | 高影响承诺不交给 Agent 自动决定 |
| 2:30–2:50 | FDE 修改上线条目、填写原因、确认三块 | 目标用户可纠正结果 |
| 2:50–3:00 | 发布，展示接手验证题 | 转换完成不等于可承诺上线 |

## 6. 页面与交互设计

### 6.1 页面 A：交接任务配置器（首页）

首页不是预置任务卡。页面上半部分是一个可编辑的任务表单；未配置完成时不出现“开始交接”。

```
┌──────────────────────── ContextBridge · Demo Replay ──────────────────────┐
│ 新建交接任务                                                               │
│ [源角色：售前人员 v]  →  [目标角色：FDE v]                                │
│                                                                            │
│ 01 Agent Memory                                                            │
│ [Agent：Codex（本地）v]                                                    │
│ Mock Memory Path · ~/.codex/agents/sales/memory/                           │
│ ☑ 售前工作记忆_陈宇.md  ☑ 售前客户跟进_陈宇.md                            │
│                                                                            │
│ 02 Knowledge Files                                                          │
│ [从本地电脑添加知识文件]                                                    │
│  ✓ 项目交接知识_澄澈零售.md                                                │
│                                                                            │
│ 03 转换要求                                                                 │
│ [把售前叙述转换为 FDE 可启动的交付 Brief；区分确认事实、待确认项、       │
│  风险和敏感信息；不要承诺上线日期。                                  ]     │
│                                                      [生成交接任务卡]      │
└───────────────────────────────────────────────────────────────────────────┘
```

功能规格：

| 区块 | 交互 | 验收要求 |
|---|---|---|
| 源角色 | 单选下拉/卡片 | 至少提供售前、客户成功、一线支持；角色改变会更新说明。 |
| 目标角色 | 单选下拉/卡片 | 至少提供 FDE、运维、客户成功；不能与源角色相同。 |
| Agent Memory | Agent + 路径勾选 | 先选择 Agent；Codex 以 Mock 目录呈现两个记忆文件，不调用或上传真实 Agent 数据。 |
| Knowledge Files | 单文件上传 | 允许 `.md,.txt,.json`；显示文件名和大小；可移除。 |
| Prompt | 多行文本，必填 | 最多 500 字；显示字符数；此 Demo 将 Prompt 展示在任务卡和审计记录中。 |
| 生成任务卡 | 校验后可点击 | 必须已选择角色、至少 1 份记忆、1 份知识和非空 Prompt。 |

### 6.2 页面 B：交接任务卡

用户点“生成交接任务卡”后，在首页下方出现；配置区域保留“编辑配置”入口。任务未生成前不显示这张卡。

```
┌────────────────────── 待执行的交接任务 ──────────────────────┐
│ 售前人员  →  FDE       澄澈零售集团 / AtlasFlow 试点          │
│ Codex 记忆路径 · 2 份记忆 · 1 份 Knowledge · 3 条人工边界     │
│ 要求：把售前叙述转换为 FDE 可启动的交付 Brief…                │
│ [编辑配置]                                      [开始交接 →]  │
└───────────────────────────────────────────────────────────────┘
```

“开始交接”只在该卡上出现；点击后进入工作台并冻结本次任务快照。用户返回首页重新编辑配置时，旧任务卡失效，需要重新生成。

### 6.3 页面 C：交接工作台（核心）

视觉方向：克制、明亮、偏 OpenAI 工作台。不要展示冗长的五段动画流水线、华丽数据卡或伪 Token 流；第一屏优先让人看清“前后语言差异”。

```
┌──────────── 源：售前的工作记忆 ────────────┬──── Bridge 决策 ────┬──── 目标：FDE 的交付记忆 ────┐
│ Codex Local Agent / sales memory            │ 4 个需要确认        │ FDE Deployment Brief            │
│                                             │ 2 个已拦截           │ 事实 · 依赖 · 风险 · 首周动作   │
│ “陈锐说现在查一次要 4 小时，最好 <1 小时。” │ ✓ 改写为验收候选      │ 业务结果：处理/初步处置 ≤ 1 小时 │
│ “邮件里先写 10/06 可以上线，问题不大。”    │ ! 人工确认            │ 风险：10/06 仅是售前预估        │
│ “我感觉陈锐未必买单。”                      │ ! 转为待验证信号      │ 待验证：发现会确认 Champion 认同 │
│ “SSO_CLIENT_SECRET=…”                       │ × 阻断                │ 依赖：索取租户、协议、测试身份   │
│                                             │                      │ [交给 FDE 检查]                 │
└─────────────────────────────────────────────┴────────────────────┴────────────────────────────────┘
```

#### 左栏：交接前 / 源角色语言

- 顶部显示来源角色、选择的真实文件名、`Codex Local Agent` 或 `Local Knowledge` 标签；
- 默认显示 `codex_sales_memory_chenyu.md` 的原始片段，不自动润色；
- 原始语句按“客户原话 / 售前承诺 / 主观判断 / 商业敏感 / 待办”着色；
- 点击任一语句，中栏定位其转换决策；
- 用户能直观看到“机会、关系、感觉、承诺混在一起”的混乱。

#### 中栏：最小 Bridge 决策

只显示 4 类动作和可点击的条目：

| 动作 | 含义 | Demo 示例 |
|---|---|---|
| 迁移 | 已确认事实直接进入目标记忆 | 首阶段不接库存/收银 |
| 改写 | 用目标职责语言表达 | “4 小时” → 验收指标候选 |
| 人工确认 | 高影响且不确定 | 10/06、内网硬约束、Champion 判断、验收准备度 |
| 阻断 | 敏感或无关 | 凭据、折扣、竞品 |

不再显示独立“流水线”或人为凑数的统计。选择条目时展示：原文、判断理由、目标文本、来源文件。

#### 右栏：交接后 / 目标角色语言

FDE Brief 固定按以下区块显示：

1. `已确认的试点范围`：运营手册、Salesforce 工单、Azure AD；排除库存/收银；
2. `交付依赖`：Azure AD 资料、Salesforce 字段/API Scope、知识文件清单、网络算力、IT Owner；
3. `风险与待确认`：上线日期冲突、私有化约束、验收基线、Champion 信号；
4. `首周动作`：技术发现会、责任人表、数据权限清单、验收基线；
5. `不迁移内容`：以计数与类别展示，不泄露秘密原文。

右栏首屏应同时出现一条“售前语言”对应的“FDE 语言”，以便录像剪辑不依赖滚动。

### 6.4 页面 D：人工确认与 FDE 检查

人工确认采用单一抽屉/弹窗，一次展示一条问题及两个可解释的选项。

FDE 检查不需要新页面：在右栏进入检查态后，范围、风险、首周动作三块出现“确认 / 标记缺口”操作，所有条目可编辑。编辑必须要求“修改理由”，结果展示 `Agent 草案 → FDE 修改` Diff 并写入审计。

### 6.5 页面 E：发布结果

发布页展示：

- “交接包已发布”；
- 接手验证题：业务目标、首个技术依赖、不能承诺什么、首周第一件事；
- 明确文案：`可启动技术发现会，不等于可承诺上线`；
- 重新演示回到空配置器，而不是自动恢复旧任务。

## 7. Mock 文件设计

### 7.1 本地文件交付位置

项目仓库保存一份受版本控制的原件：`demo-inputs/`。

知识文件复制到用户桌面：`~/Desktop/ContextBridge Demo Inputs/`，以便录屏时通过系统文件选择器实际选择；两份记忆文件在产品内模拟为 Codex 记忆路径下的可勾选文件。所有数据都是虚构的，不连接任何真实企业系统。

| 文件 | 选择入口 | 内容目的 |
|---|---|---|
| `售前工作记忆_陈宇.md` | Codex Mock Memory Path | 售前口吻：价值、非正式上线预估、内网判断、伪凭据、商业敏感。 |
| `售前客户跟进_陈宇.md` | Codex Mock Memory Path | 售前跟进线索：预算决策人偏好、5 家门店试点和未定验收样本。 |
| `项目交接知识_澄澈零售.md` | Knowledge Files | 已确认范围、业务目标、FDE 依赖缺口、排期边界。 |

### 7.2 样例对照（必须在 UI 中可见）

| 源角色原话 | Bridge 判断 | FDE 接收语言 |
|---|---|---|
| “陈锐说现在查一次要 4 小时，最好先做到 1 小时内，客户才有感觉。” | 改写；指标仍待定义基线 | `验收候选：门店/售后问题处理的检索与初步处置 ≤ 1 小时；与业务 Owner 确认基线和抽样。` |
| “邮件里先写 10/06 可上线，问题应该不大。” | 人工确认；与会议计划冲突 | `10/06 为售前预估，不构成交付承诺；当前参考为 10/13 启动两周试点。` |
| “我感觉陈锐对价值还没完全信。” | 人工确认；主观判断不能变事实 | `待验证信号：首次发现会确认业务 Champion 的试点价值认同度。` |
| `SSO_CLIENT_SECRET=atlas-demo-secret` | 阻断 | `不迁移凭据；向客户 IT 索取 Azure AD 租户、协议和测试身份。` |
| “CRM 折扣不低于 18%，竞品是 ExampleAssist。” | 阻断 | `无输出。` |

## 8. 功能 Spec

### 8.1 领域对象

```ts
type RoleId = 'sales' | 'csm' | 'support' | 'fde' | 'ops';

type LocalFile = {
  id: string;
  name: string;
  kind: 'agent_memory' | 'knowledge';
  origin: 'codex_local_agent' | 'desktop_upload' | 'demo_fixture';
  pathLabel: string;
  content: string;
  size: number;
};

type TransferTask = {
  id: string;
  sourceRole: RoleId;
  targetRole: RoleId;
  memoryFiles: LocalFile[];
  knowledgeFiles: LocalFile[];
  prompt: string;
  status: 'draft' | 'ready' | 'running' | 'review' | 'target_check' | 'published';
};
```

### 8.2 状态机

```text
configuration incomplete
  → draft (inputs valid)
  → ready (task card generated)
  → running (start task)
  → review (draft generated)
  → target_check (human review done)
  → published

Any config change after ready → draft and invalidates task card.
```

### 8.3 转换规则优先级

1. `block`：凭据、商业价格、竞品等敏感数据永不进入目标 Brief；
2. `review`：上线日期、范围外承诺、部署硬约束、主观判断、验收标准缺失；
3. `transfer`：会议中确认的范围、客户角色、明确排除项；
4. `reframe`：把业务愿望、待办、技术问卷转换为目标角色可行动语言；
5. `prompt`：仅影响目标语言的表达与强调项，不能覆盖 1–4 的治理边界。

### 8.4 完成条件

- 已选择不同的源/目标角色；
- 至少 1 份记忆、1 份知识、非空 Prompt；
- 3 条默认人工确认（上线日期、内网部署、主观判断）全部处理；
- FDE 完成范围、风险、首周动作三项检查；
- 任何 FDE 编辑均有理由。

## 9. 验收标准

| ID | 验收场景 | 预期 |
|---|---|---|
| AC-01 | 初次打开首页 | 只出现配置器，不出现预置交接任务卡或开始按钮。 |
| AC-02 | 填完配置 | 才能生成任务卡；角色、文件数、Prompt 摘要正确。 |
| AC-03 | 选择 Codex 和其 Mock 记忆路径；从桌面选一份知识 `.md` | 记忆文件显示 `Codex · ~/.codex/agents/sales/memory/`；知识文件显示本地上传标签，内容均可见。 |
| AC-04 | 点击开始交接 | 进入任务快照对应的工作台。 |
| AC-05 | 查看工作台 | 源栏是售前原话，目标栏是 FDE 依赖/风险/动作，而非同义摘要。 |
| AC-06 | 查看 secret/价格 | 源栏可见测试数据，但目标 Brief 和发布页不含原文。 |
| AC-07 | 处理 10/06 | 默认不能自动转为上线承诺；需人工选择。 |
| AC-08 | FDE 编辑 | 必填理由，保存后可见 Diff 与审计。 |
| AC-09 | 无网络运行 | 使用 Demo Replay 完成全链路，不要求 Key。 |

## 10. 面试题加分项

### 10.1 故意保留人工的一步

把“对外上线日期、部署硬约束、主观客户判断、验收准备度”留给人工确认。它们影响合同、责任与上线风险，模型可标记证据与冲突，却不应替企业做承诺。

### 10.2 最容易出错及验证

最易错的是把售前的“可能、感觉、预计”固化成 FDE 的事实。Demo 用原文与目标语言并排、来源文件、置信/规则标签和接手验证题验证；自动化测试断言凭据、折扣、竞品不进入 Brief，且 10/06 仅在人工确认后变为参考信息。

### 10.3 若客户要求下周上线

最难补的不是界面，而是可信且有责任人的实时数据和组织流程：身份、网络、数据权限、客户 IT Owner、接口 Scope、验收基线、环境资源、脱敏、审计、回滚与评测。POC 证明的是交接工作流，不伪装为生产就绪。
